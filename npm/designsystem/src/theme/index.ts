import { palette } from './palette';
import { spacer, spacers } from './spacers';
import { screen, breakpoints } from './grid';

export const theme = {
  spacer,
  spacers,
  palette,
  screen,
  breakpoints,
};
