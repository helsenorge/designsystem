export enum FormExampleVariants {
  formgroup = 'formgroup',
  checkbox = 'checkbox',
  radiobutton = 'radiobutton',
  textarea = 'textarea',
  input = 'input',
  select = 'select',
  slider = 'slider',
  withoutformgroup = 'withoutformgroup',
}
