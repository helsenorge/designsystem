---
title: Validation
path: /bibliotek/komponenter/Validation
date: 2021-02-12
---

import MDXCustom from '../../../../components/markdown/MDXCustom';
import * as propData from '@helsenorge/designsystem-react/components/Validation/componentdata.json';
import Button from '@helsenorge/designsystem-react/components/Button';
import Checkbox from '@helsenorge/designsystem-react/components/Checkbox';
import FormGroup from '@helsenorge/designsystem-react/components/FormGroup';
import Validation from '@helsenorge/designsystem-react/components/Validation';
import FormExample from '@helsenorge/designsystem-react/components/FormExample';

# Validation

<MDXCustom.Preamble>
  Et validation komponent som wrapper enten FormGroup eller et individuelt form input komponent.
</MDXCustom.Preamble>

## Usage
```javascript
import React from 'react';

import { useForm } from 'react-hook-form';

import Validation from '@helsenorge/designsystem-react/components/Validation';
import FormGroup, { FormGroupModes, FormGroupVariants } from '@helsenorge/designsystem-react/components/FormGroup';
import Checkbox from '@helsenorge/designsystem-react/components/Checkbox';
import Button from '@helsenorge/designsystem-react/components/Button';

export const FormExample = React.forwardRef((props, ref: React.ForwardedRef<HTMLElement>) => {
  const { register, handleSubmit, errors } = useForm();
  const formGroup1 = 'formgroup1';
  const formGroup2 = 'formgroup2';
  const allErrors = errors[formGroup1] || errors[formGroup2];
  const errorMessage = 'Du må velge et alternativ';
  const errorMessage2 = 'Du må velge to alternativ';

  const requireTwo = (value: Array<string>) => {
    return value.length >= 2 || errorMessage2;
  };

  return (
    <form
      onSubmit={handleSubmit(data => {
        console.log(data);
      })}
    >
      <Validation variant={props.variant} errorSummary={allErrors ? 'Sjekk at alt er riktig utfylt' : undefined}>
        <FormGroup
          key={0}
          title={'One amazing title'}
          legend={'Check out these checkboxes!'}
          error={errors[formGroup1] ? errors[formGroup1].message : undefined}
          variant={props.variant}
          name={formGroup1}
        >
          <Checkbox label={'Checkbox 1'} ref={register({ required: errorMessage })} />
          <Checkbox label={'Checkbox 2'} ref={register({ required: errorMessage })} />
          <Checkbox label={'Checkbox 3'} ref={register({ required: errorMessage })} />
        </FormGroup>,
        <FormGroup
          key={1}
          legend={'Select one options!'}
          error={errors[formGroup3] ? errors[formGroup3].message : undefined}
          variant={props.variant}
          name={formGroup3}
        >
          <RadioButton label={'Radiobutton 1'} ref={register({ required: errorMessage })} />
          <RadioButton label={'Radiobutton 2'} ref={register({ required: errorMessage })} />
          <RadioButton label={'Radiobutton 3'} ref={register({ required: errorMessage })} />
        </FormGroup>,
        <FormGroup key={2} error={errors[formGroup4] ? errors[formGroup4].message : undefined}>
          <Textarea
            defaultValue={`Dette er en test \n\n Hello \n\n test \n\n test test`}
            grow
            maxCharacters={40}
            minRows={5}
            label="Skriv din historie her"
            name={formGroup4}
            ref={register({ maxLength: { value: 40, message: errorMessage3 } })}
          />
        </FormGroup>
      </Validation>
      <br />
      <Button>{'Submit'}</Button>
    </form>
  );
});
```

## Preview

<MDXCustom.LiveComponent scope={{ FormExample }}>
  {`
  <div style={
    {padding: '20px',
    width: '30vw'}}>
    <FormExample exampleType={'formgroup'}></FormExample>
    </div>
  `}
</MDXCustom.LiveComponent>

## Properties

<MDXCustom.ComponentProps propData={{ propData }} />

## Bruk

**TODO: Legg til bruk**

### Tilgjengelighet

**TODO: Legg til UU**
