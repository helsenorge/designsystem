import styled from 'styled-components';

const Preamble = styled('p')`
  font-weight: 300;
  margin-top: 2.25rem;
  margin-bottom: 2rem;
  line-height: 2rem;
  font-size: 1.3rem;
`;

export default Preamble;
