import styled from 'styled-components';

const Preamble = styled('p')`
  font-weight: 600;
  margin-top: 2.25rem;
  margin-bottom: 2rem;
  line-height: 2rem;
  font-size: 1.125rem;
`;

export default Preamble;
