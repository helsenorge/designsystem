---
title: Icon
path: /library/components/icon
date: 2019-01-29
---

import MDXCustom from '../../../../components/markdown/MDXCustom';
import * as propData from './icon.json';
import Icon from '@helsenorge/designsystem-react/components/Icons';
import Pencil from '@helsenorge/designsystem-react/components/Icons/Pencil';
import AlarmClock from '@helsenorge/designsystem-react/components/Icons/AlarmClock';
import Avatar from '@helsenorge/designsystem-react/components/Icons/Avatar';

# Icon

<MDXCustom.Preamble>
  He’s not a word hero. He’s a word hero because he was captured. I like text that wasn’t captured. Lorem Ipsum is the
  single greatest threat.
</MDXCustom.Preamble>

## Preview

<MDXCustom.LiveComponent scope={{Icon}}>{`<Icon svgIcon={Pencil} />`}</MDXCustom.LiveComponent>

## Properties

<MDXCustom.ComponentProps propData={{propData}} />

## Examples

### Sizes

<MDXCustom.LiveComponent stack scope={{Icon}}>{`
  <Icon svgIcon={Pencil} size={16} />
  <Icon svgIcon={Pencil} size={32} />
  <Icon svgIcon={Pencil} size={64} />
  <Icon svgIcon={Pencil} size={128} />
`}</MDXCustom.LiveComponent>

### Colors

<MDXCustom.LiveComponent scope={{Icon}}>{`
  <Icon svgIcon={Pencil} color="black" />
  <Icon svgIcon={Pencil} color="white" />
  <Icon svgIcon={Pencil} color="banana" />
  <Icon svgIcon={Pencil} color="blueberry" />
  <Icon svgIcon={Pencil} color="cherry" />
  <Icon svgIcon={Pencil} color="kiwi" />
  <Icon svgIcon={Pencil} color="plum" />
`}</MDXCustom.LiveComponent>

### Hover

<MDXCustom.LiveComponent scope={{Icon}}>{`
  <Icon svgIcon={AlarmClock} />
  <Icon svgIcon={Pencil} />
  <Icon svgIcon={Avatar} />
  <Icon isHovered svgIcon={AlarmClock} />
  <Icon isHovered svgIcon={Pencil} />
  <Icon isHovered svgIcon={Avatar} />
`}</MDXCustom.LiveComponent>

## Bruk

**TODO: Legg til bruk**

### Tilgjengelighet

**TODO: Legg til UU**
